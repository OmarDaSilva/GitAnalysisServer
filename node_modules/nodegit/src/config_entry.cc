// This is a generated file, modify: generate/templates/templates/class_content.cc

#include <nan.h>
#include <string.h>

extern "C" {
  #include <git2.h>
 }

#include "../include/nodegit.h"
#include "../include/lock_master.h"
#include "../include/functions/copy.h"
#include "../include/config_entry.h"
#include "nodegit_wrapper.cc"

 
#include <iostream>

using namespace std;
using namespace v8;
using namespace node;

  GitConfigEntry::~GitConfigEntry() {
    // this will cause an error if you have a non-self-freeing object that also needs
    // to save values. Since the object that will eventually free the object has no
    // way of knowing to free these values.
   }

  void GitConfigEntry::InitializeComponent(v8::Local<v8::Object> target, nodegit::Context *nodegitContext) {
    Nan::HandleScope scope;

    v8::Local<v8::External> nodegitExternal = Nan::New<v8::External>(nodegitContext);
    v8::Local<FunctionTemplate> tpl = Nan::New<FunctionTemplate>(JSNewFunction, nodegitExternal);

    tpl->InstanceTemplate()->SetInternalFieldCount(1);
    tpl->SetClassName(Nan::New("ConfigEntry").ToLocalChecked());

         Nan::SetPrototypeMethod(tpl, "name", Name, nodegitExternal);
         Nan::SetPrototypeMethod(tpl, "value", Value, nodegitExternal);
         Nan::SetPrototypeMethod(tpl, "level", Level, nodegitExternal);
  
    InitializeTemplate(tpl);

    v8::Local<Function> constructor_template = Nan::GetFunction(tpl).ToLocalChecked();
    nodegitContext->SaveToPersistent("GitConfigEntry::Template", constructor_template);
    Nan::Set(target, Nan::New("ConfigEntry").ToLocalChecked(), constructor_template);
  }

      // start field block
    NAN_METHOD(GitConfigEntry::Name) {
      v8::Local<v8::Value> v8ConversionSlot;

            const char *
           name =
          Nan::ObjectWrap::Unwrap<GitConfigEntry>(info.This())->GetValue()->name;
 // start convert_to_v8 block
  if (name){
       v8ConversionSlot = Nan::New<v8::String>(name).ToLocalChecked();
   }
  else {
    v8ConversionSlot = Nan::Null();
  }

  // end convert_to_v8 block
      info.GetReturnValue().Set(v8ConversionSlot);
    }
    // end field block
     // start field block
    NAN_METHOD(GitConfigEntry::Value) {
      v8::Local<v8::Value> v8ConversionSlot;

            const char *
           value =
          Nan::ObjectWrap::Unwrap<GitConfigEntry>(info.This())->GetValue()->value;
 // start convert_to_v8 block
  if (value){
       v8ConversionSlot = Nan::New<v8::String>(value).ToLocalChecked();
   }
  else {
    v8ConversionSlot = Nan::Null();
  }

  // end convert_to_v8 block
      info.GetReturnValue().Set(v8ConversionSlot);
    }
    // end field block
     // start field block
    NAN_METHOD(GitConfigEntry::Level) {
      v8::Local<v8::Value> v8ConversionSlot;

            git_config_level_t
           level =
          Nan::ObjectWrap::Unwrap<GitConfigEntry>(info.This())->GetValue()->level;
 // start convert_to_v8 block
     v8ConversionSlot = Nan::New<Number>( level);
  // end convert_to_v8 block
      info.GetReturnValue().Set(v8ConversionSlot);
    }
    // end field block
  // force base class template instantiation, to make sure we get all the
// methods, statics, etc.
template class NodeGitWrapper<GitConfigEntryTraits>;
 