// This is a generated file, modify: generate/templates/templates/struct_content.cc

// generated from struct_content.cc
#include <nan.h>
#include <string.h>
#ifdef WIN32
#include <windows.h>
#else
#include <unistd.h>
#endif // win32

extern "C" {
  #include <git2.h>
 }

#include <iostream>
#include "../include/nodegit.h"
#include "../include/lock_master.h"
#include "../include/functions/copy.h"
#include "../include/merge_file_options.h"
#include "nodegit_wrapper.cc"

 
using namespace v8;
using namespace node;
using namespace std;

 
ConfigurableGitMergeFileOptions::ConfigurableGitMergeFileOptions(nodegit::Context *nodegitContext)
  : nodegit::ConfigurableClassWrapper<GitMergeFileOptionsTraits>(nodegitContext)
{
     git_merge_file_options  wrappedValue = GIT_MERGE_FILE_OPTIONS_INIT;
    this->raw = (git_merge_file_options*) malloc(sizeof(git_merge_file_options ));
    memcpy(this->raw, &wrappedValue, sizeof(git_merge_file_options ));
 }

ConfigurableGitMergeFileOptions::~ConfigurableGitMergeFileOptions() {
          delete this->raw->ancestor_label;
          delete this->raw->our_label;
          delete this->raw->their_label;
         }

nodegit::ConfigurableClassWrapper<GitMergeFileOptionsTraits>::v8ConversionResult ConfigurableGitMergeFileOptions::fromJavascript(nodegit::Context *nodegitContext, v8::Local<v8::Value> input) {
  if (!input->IsObject()) {
    return {
      "Must pass object for ConfigurableGitMergeFileOptions"
    };
  }

  Nan::HandleScope scope;
  v8::Local<v8::Object> inputObj = input.As<v8::Object>();
  std::shared_ptr<ConfigurableGitMergeFileOptions> output(new ConfigurableGitMergeFileOptions(nodegitContext));

  // unpack the data into the correct fields
         {
          v8::Local<v8::Value> maybeNumber = nodegit::safeGetField(inputObj, "version");
          if (!maybeNumber.IsEmpty() && !maybeNumber->IsUndefined() && !maybeNumber->IsNull()) {
            if (!maybeNumber->IsNumber()) {
              return {
                "Must pass Int32 to version"
              };
            }

            output->raw->version = static_cast<unsigned int>(maybeNumber->Int32Value(Nan::GetCurrentContext()).FromJust());
          }
        }
          output->raw->ancestor_label = nullptr;
        {
          v8::Local<v8::Value> maybeString = nodegit::safeGetField(inputObj, "ancestorLabel");
          if (!maybeString.IsEmpty() && !maybeString->IsUndefined() && !maybeString->IsNull()) {
            if (!maybeString->IsString()) {
              return {
                "Must pass string to ancestorLabel"
              };
            }

            Nan::Utf8String utf8String(maybeString.As<v8::String>());
            output->raw->ancestor_label = strdup(*utf8String);
          }
        }
          output->raw->our_label = nullptr;
        {
          v8::Local<v8::Value> maybeString = nodegit::safeGetField(inputObj, "ourLabel");
          if (!maybeString.IsEmpty() && !maybeString->IsUndefined() && !maybeString->IsNull()) {
            if (!maybeString->IsString()) {
              return {
                "Must pass string to ourLabel"
              };
            }

            Nan::Utf8String utf8String(maybeString.As<v8::String>());
            output->raw->our_label = strdup(*utf8String);
          }
        }
          output->raw->their_label = nullptr;
        {
          v8::Local<v8::Value> maybeString = nodegit::safeGetField(inputObj, "theirLabel");
          if (!maybeString.IsEmpty() && !maybeString->IsUndefined() && !maybeString->IsNull()) {
            if (!maybeString->IsString()) {
              return {
                "Must pass string to theirLabel"
              };
            }

            Nan::Utf8String utf8String(maybeString.As<v8::String>());
            output->raw->their_label = strdup(*utf8String);
          }
        }
           {
          v8::Local<v8::Value> maybeNumber = nodegit::safeGetField(inputObj, "favor");
          if (!maybeNumber.IsEmpty() && !maybeNumber->IsUndefined() && !maybeNumber->IsNull()) {
            if (!maybeNumber->IsNumber()) {
              return {
                "Must pass Int32 to favor"
              };
            }

            output->raw->favor = static_cast<git_merge_file_favor_t>(maybeNumber->Int32Value(Nan::GetCurrentContext()).FromJust());
          }
        }
           {
          v8::Local<v8::Value> maybeNumber = nodegit::safeGetField(inputObj, "flags");
          if (!maybeNumber.IsEmpty() && !maybeNumber->IsUndefined() && !maybeNumber->IsNull()) {
            if (!maybeNumber->IsNumber()) {
              return {
                "Must pass Int32 to flags"
              };
            }

            output->raw->flags = static_cast<uint32_t>(maybeNumber->Int32Value(Nan::GetCurrentContext()).FromJust());
          }
        }
           {
          v8::Local<v8::Value> maybeNumber = nodegit::safeGetField(inputObj, "markerSize");
          if (!maybeNumber.IsEmpty() && !maybeNumber->IsUndefined() && !maybeNumber->IsNull()) {
            if (!maybeNumber->IsNumber()) {
              return {
                "Must pass Int32 to markerSize"
              };
            }

            output->raw->marker_size = static_cast<unsigned short>(maybeNumber->Int32Value(Nan::GetCurrentContext()).FromJust());
          }
        }
    
  return {
    output
  };
}

               
// force base class template instantiation, to make sure we get all the
// methods, statics, etc.
template class nodegit::ConfigurableClassWrapper<GitMergeFileOptionsTraits>;
