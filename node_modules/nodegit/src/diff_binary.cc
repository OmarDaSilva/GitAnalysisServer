// This is a generated file, modify: generate/templates/templates/class_content.cc

#include <nan.h>
#include <string.h>

extern "C" {
  #include <git2.h>
 }

#include "../include/nodegit.h"
#include "../include/lock_master.h"
#include "../include/functions/copy.h"
#include "../include/diff_binary.h"
#include "nodegit_wrapper.cc"

  #include "../include/diff_binary_file.h"
 
#include <iostream>

using namespace std;
using namespace v8;
using namespace node;

  GitDiffBinary::~GitDiffBinary() {
    // this will cause an error if you have a non-self-freeing object that also needs
    // to save values. Since the object that will eventually free the object has no
    // way of knowing to free these values.
   }

  void GitDiffBinary::InitializeComponent(v8::Local<v8::Object> target, nodegit::Context *nodegitContext) {
    Nan::HandleScope scope;

    v8::Local<v8::External> nodegitExternal = Nan::New<v8::External>(nodegitContext);
    v8::Local<FunctionTemplate> tpl = Nan::New<FunctionTemplate>(JSNewFunction, nodegitExternal);

    tpl->InstanceTemplate()->SetInternalFieldCount(1);
    tpl->SetClassName(Nan::New("DiffBinary").ToLocalChecked());

         Nan::SetPrototypeMethod(tpl, "containsData", ContainsData, nodegitExternal);
         Nan::SetPrototypeMethod(tpl, "oldFile", OldFile, nodegitExternal);
         Nan::SetPrototypeMethod(tpl, "newFile", NewFile, nodegitExternal);
  
    InitializeTemplate(tpl);

    v8::Local<Function> constructor_template = Nan::GetFunction(tpl).ToLocalChecked();
    nodegitContext->SaveToPersistent("GitDiffBinary::Template", constructor_template);
    Nan::Set(target, Nan::New("DiffBinary").ToLocalChecked(), constructor_template);
  }

      // start field block
    NAN_METHOD(GitDiffBinary::ContainsData) {
      v8::Local<v8::Value> v8ConversionSlot;

            unsigned int
           contains_data =
          Nan::ObjectWrap::Unwrap<GitDiffBinary>(info.This())->GetValue()->contains_data;
 // start convert_to_v8 block
     v8ConversionSlot = Nan::New<Number>( contains_data);
  // end convert_to_v8 block
      info.GetReturnValue().Set(v8ConversionSlot);
    }
    // end field block
     // start field block
    NAN_METHOD(GitDiffBinary::OldFile) {
      v8::Local<v8::Value> v8ConversionSlot;

            git_diff_binary_file
*            old_file =
          &
           Nan::ObjectWrap::Unwrap<GitDiffBinary>(info.This())->GetValue()->old_file;
 // start convert_to_v8 block
    if (old_file != NULL) {
      v8::Local<v8::Array> owners = Nan::New<Array>(0);
         Nan::Set(owners, owners->Length(), info.This());
          v8ConversionSlot = GitDiffBinaryFile::New(
        old_file,
        false
          , owners
       );
   }
  else {
    v8ConversionSlot = Nan::Null();
  }
  // end convert_to_v8 block
      info.GetReturnValue().Set(v8ConversionSlot);
    }
    // end field block
     // start field block
    NAN_METHOD(GitDiffBinary::NewFile) {
      v8::Local<v8::Value> v8ConversionSlot;

            git_diff_binary_file
*            new_file =
          &
           Nan::ObjectWrap::Unwrap<GitDiffBinary>(info.This())->GetValue()->new_file;
 // start convert_to_v8 block
    if (new_file != NULL) {
      v8::Local<v8::Array> owners = Nan::New<Array>(0);
         Nan::Set(owners, owners->Length(), info.This());
          v8ConversionSlot = GitDiffBinaryFile::New(
        new_file,
        false
          , owners
       );
   }
  else {
    v8ConversionSlot = Nan::Null();
  }
  // end convert_to_v8 block
      info.GetReturnValue().Set(v8ConversionSlot);
    }
    // end field block
  // force base class template instantiation, to make sure we get all the
// methods, statics, etc.
template class NodeGitWrapper<GitDiffBinaryTraits>;
 