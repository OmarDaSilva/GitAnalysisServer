// This is a generated file, modify: generate/templates/templates/class_content.cc

#include <nan.h>
#include <string.h>

extern "C" {
  #include <git2.h>
 }

#include "../include/nodegit.h"
#include "../include/lock_master.h"
#include "../include/functions/copy.h"
#include "../include/attr_options.h"
#include "nodegit_wrapper.cc"

  #include "../include/oid.h"
 
#include <iostream>

using namespace std;
using namespace v8;
using namespace node;

  GitAttrOptions::~GitAttrOptions() {
    // this will cause an error if you have a non-self-freeing object that also needs
    // to save values. Since the object that will eventually free the object has no
    // way of knowing to free these values.
   }

  void GitAttrOptions::InitializeComponent(v8::Local<v8::Object> target, nodegit::Context *nodegitContext) {
    Nan::HandleScope scope;

    v8::Local<v8::External> nodegitExternal = Nan::New<v8::External>(nodegitContext);
    v8::Local<FunctionTemplate> tpl = Nan::New<FunctionTemplate>(JSNewFunction, nodegitExternal);

    tpl->InstanceTemplate()->SetInternalFieldCount(1);
    tpl->SetClassName(Nan::New("AttrOptions").ToLocalChecked());

         Nan::SetPrototypeMethod(tpl, "version", Version, nodegitExternal);
         Nan::SetPrototypeMethod(tpl, "flags", Flags, nodegitExternal);
         Nan::SetPrototypeMethod(tpl, "commitId", CommitId, nodegitExternal);
         Nan::SetPrototypeMethod(tpl, "attrCommitId", AttrCommitId, nodegitExternal);
  
    InitializeTemplate(tpl);

    v8::Local<Function> constructor_template = Nan::GetFunction(tpl).ToLocalChecked();
    nodegitContext->SaveToPersistent("GitAttrOptions::Template", constructor_template);
    Nan::Set(target, Nan::New("AttrOptions").ToLocalChecked(), constructor_template);
  }

      // start field block
    NAN_METHOD(GitAttrOptions::Version) {
      v8::Local<v8::Value> v8ConversionSlot;

            unsigned int
           version =
          Nan::ObjectWrap::Unwrap<GitAttrOptions>(info.This())->GetValue()->version;
 // start convert_to_v8 block
     v8ConversionSlot = Nan::New<Number>( version);
  // end convert_to_v8 block
      info.GetReturnValue().Set(v8ConversionSlot);
    }
    // end field block
     // start field block
    NAN_METHOD(GitAttrOptions::Flags) {
      v8::Local<v8::Value> v8ConversionSlot;

            unsigned int
           flags =
          Nan::ObjectWrap::Unwrap<GitAttrOptions>(info.This())->GetValue()->flags;
 // start convert_to_v8 block
     v8ConversionSlot = Nan::New<Number>( flags);
  // end convert_to_v8 block
      info.GetReturnValue().Set(v8ConversionSlot);
    }
    // end field block
     // start field block
    NAN_METHOD(GitAttrOptions::CommitId) {
      v8::Local<v8::Value> v8ConversionSlot;

            git_oid *
            commit_id =
           Nan::ObjectWrap::Unwrap<GitAttrOptions>(info.This())->GetValue()->commit_id;
 // start convert_to_v8 block
    if (commit_id != NULL) {
      v8::Local<v8::Array> owners = Nan::New<Array>(0);
         Nan::Set(owners, owners->Length(), info.This());
          v8ConversionSlot = GitOid::New(
        commit_id,
        true
          , owners
       );
   }
  else {
    v8ConversionSlot = Nan::Null();
  }
  // end convert_to_v8 block
      info.GetReturnValue().Set(v8ConversionSlot);
    }
    // end field block
     // start field block
    NAN_METHOD(GitAttrOptions::AttrCommitId) {
      v8::Local<v8::Value> v8ConversionSlot;

            git_oid
*            attr_commit_id =
          &
           Nan::ObjectWrap::Unwrap<GitAttrOptions>(info.This())->GetValue()->attr_commit_id;
 // start convert_to_v8 block
    if (attr_commit_id != NULL) {
      v8::Local<v8::Array> owners = Nan::New<Array>(0);
         Nan::Set(owners, owners->Length(), info.This());
          v8ConversionSlot = GitOid::New(
        attr_commit_id,
        true
          , owners
       );
   }
  else {
    v8ConversionSlot = Nan::Null();
  }
  // end convert_to_v8 block
      info.GetReturnValue().Set(v8ConversionSlot);
    }
    // end field block
  // force base class template instantiation, to make sure we get all the
// methods, statics, etc.
template class NodeGitWrapper<GitAttrOptionsTraits>;
 