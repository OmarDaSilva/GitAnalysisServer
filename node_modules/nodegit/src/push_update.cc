// This is a generated file, modify: generate/templates/templates/class_content.cc

#include <nan.h>
#include <string.h>

extern "C" {
  #include <git2.h>
 }

#include "../include/nodegit.h"
#include "../include/lock_master.h"
#include "../include/functions/copy.h"
#include "../include/push_update.h"
#include "nodegit_wrapper.cc"

  #include "../include/oid.h"
 
#include <iostream>

using namespace std;
using namespace v8;
using namespace node;

  GitPushUpdate::~GitPushUpdate() {
    // this will cause an error if you have a non-self-freeing object that also needs
    // to save values. Since the object that will eventually free the object has no
    // way of knowing to free these values.
   }

  void GitPushUpdate::InitializeComponent(v8::Local<v8::Object> target, nodegit::Context *nodegitContext) {
    Nan::HandleScope scope;

    v8::Local<v8::External> nodegitExternal = Nan::New<v8::External>(nodegitContext);
    v8::Local<FunctionTemplate> tpl = Nan::New<FunctionTemplate>(JSNewFunction, nodegitExternal);

    tpl->InstanceTemplate()->SetInternalFieldCount(1);
    tpl->SetClassName(Nan::New("PushUpdate").ToLocalChecked());

         Nan::SetPrototypeMethod(tpl, "srcRefname", SrcRefname, nodegitExternal);
         Nan::SetPrototypeMethod(tpl, "dstRefname", DstRefname, nodegitExternal);
         Nan::SetPrototypeMethod(tpl, "src", Src, nodegitExternal);
         Nan::SetPrototypeMethod(tpl, "dst", Dst, nodegitExternal);
  
    InitializeTemplate(tpl);

    v8::Local<Function> constructor_template = Nan::GetFunction(tpl).ToLocalChecked();
    nodegitContext->SaveToPersistent("GitPushUpdate::Template", constructor_template);
    Nan::Set(target, Nan::New("PushUpdate").ToLocalChecked(), constructor_template);
  }

      // start field block
    NAN_METHOD(GitPushUpdate::SrcRefname) {
      v8::Local<v8::Value> v8ConversionSlot;

            char *
           src_refname =
          Nan::ObjectWrap::Unwrap<GitPushUpdate>(info.This())->GetValue()->src_refname;
 // start convert_to_v8 block
  if (src_refname){
       v8ConversionSlot = Nan::New<v8::String>(src_refname).ToLocalChecked();
   }
  else {
    v8ConversionSlot = Nan::Null();
  }

  // end convert_to_v8 block
      info.GetReturnValue().Set(v8ConversionSlot);
    }
    // end field block
     // start field block
    NAN_METHOD(GitPushUpdate::DstRefname) {
      v8::Local<v8::Value> v8ConversionSlot;

            char *
           dst_refname =
          Nan::ObjectWrap::Unwrap<GitPushUpdate>(info.This())->GetValue()->dst_refname;
 // start convert_to_v8 block
  if (dst_refname){
       v8ConversionSlot = Nan::New<v8::String>(dst_refname).ToLocalChecked();
   }
  else {
    v8ConversionSlot = Nan::Null();
  }

  // end convert_to_v8 block
      info.GetReturnValue().Set(v8ConversionSlot);
    }
    // end field block
     // start field block
    NAN_METHOD(GitPushUpdate::Src) {
      v8::Local<v8::Value> v8ConversionSlot;

            git_oid
*            src =
          &
           Nan::ObjectWrap::Unwrap<GitPushUpdate>(info.This())->GetValue()->src;
 // start convert_to_v8 block
    if (src != NULL) {
      v8::Local<v8::Array> owners = Nan::New<Array>(0);
         Nan::Set(owners, owners->Length(), info.This());
          v8ConversionSlot = GitOid::New(
        src,
        true
          , owners
       );
   }
  else {
    v8ConversionSlot = Nan::Null();
  }
  // end convert_to_v8 block
      info.GetReturnValue().Set(v8ConversionSlot);
    }
    // end field block
     // start field block
    NAN_METHOD(GitPushUpdate::Dst) {
      v8::Local<v8::Value> v8ConversionSlot;

            git_oid
*            dst =
          &
           Nan::ObjectWrap::Unwrap<GitPushUpdate>(info.This())->GetValue()->dst;
 // start convert_to_v8 block
    if (dst != NULL) {
      v8::Local<v8::Array> owners = Nan::New<Array>(0);
         Nan::Set(owners, owners->Length(), info.This());
          v8ConversionSlot = GitOid::New(
        dst,
        true
          , owners
       );
   }
  else {
    v8ConversionSlot = Nan::Null();
  }
  // end convert_to_v8 block
      info.GetReturnValue().Set(v8ConversionSlot);
    }
    // end field block
  // force base class template instantiation, to make sure we get all the
// methods, statics, etc.
template class NodeGitWrapper<GitPushUpdateTraits>;
 