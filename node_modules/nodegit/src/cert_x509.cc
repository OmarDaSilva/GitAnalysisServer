// This is a generated file, modify: generate/templates/templates/class_content.cc

#include <nan.h>
#include <string.h>

extern "C" {
  #include <git2.h>
 }

#include "../include/nodegit.h"
#include "../include/lock_master.h"
#include "../include/functions/copy.h"
#include "../include/cert_x509.h"
#include "nodegit_wrapper.cc"

  #include "../include/wrapper.h"
  #include "node_buffer.h"
  #include "../include/cert.h"
 
#include <iostream>

using namespace std;
using namespace v8;
using namespace node;

  GitCertX509::~GitCertX509() {
    // this will cause an error if you have a non-self-freeing object that also needs
    // to save values. Since the object that will eventually free the object has no
    // way of knowing to free these values.
   }

  void GitCertX509::InitializeComponent(v8::Local<v8::Object> target, nodegit::Context *nodegitContext) {
    Nan::HandleScope scope;

    v8::Local<v8::External> nodegitExternal = Nan::New<v8::External>(nodegitContext);
    v8::Local<FunctionTemplate> tpl = Nan::New<FunctionTemplate>(JSNewFunction, nodegitExternal);

    tpl->InstanceTemplate()->SetInternalFieldCount(1);
    tpl->SetClassName(Nan::New("CertX509").ToLocalChecked());

         Nan::SetPrototypeMethod(tpl, "parent", Parent, nodegitExternal);
         Nan::SetPrototypeMethod(tpl, "data", Data, nodegitExternal);
         Nan::SetPrototypeMethod(tpl, "len", Len, nodegitExternal);
  
    InitializeTemplate(tpl);

    v8::Local<Function> constructor_template = Nan::GetFunction(tpl).ToLocalChecked();
    nodegitContext->SaveToPersistent("GitCertX509::Template", constructor_template);
    Nan::Set(target, Nan::New("CertX509").ToLocalChecked(), constructor_template);
  }

      // start field block
    NAN_METHOD(GitCertX509::Parent) {
      v8::Local<v8::Value> v8ConversionSlot;

            git_cert
*            parent =
          &
           Nan::ObjectWrap::Unwrap<GitCertX509>(info.This())->GetValue()->parent;
 // start convert_to_v8 block
    if (parent != NULL) {
      v8::Local<v8::Array> owners = Nan::New<Array>(0);
         Nan::Set(owners, owners->Length(), info.This());
          v8ConversionSlot = GitCert::New(
        parent,
        false
          , owners
       );
   }
  else {
    v8ConversionSlot = Nan::Null();
  }
  // end convert_to_v8 block
      info.GetReturnValue().Set(v8ConversionSlot);
    }
    // end field block
     // start field block
    NAN_METHOD(GitCertX509::Data) {
      v8::Local<v8::Value> v8ConversionSlot;

            void *
            data =
           Nan::ObjectWrap::Unwrap<GitCertX509>(info.This())->GetValue()->data;
 // start convert_to_v8 block
    if (data != NULL) {
      v8::Local<v8::Array> owners = Nan::New<Array>(0);
         Nan::Set(owners, owners->Length(), info.This());
         v8ConversionSlot = Wrapper::New(data);
   }
  else {
    v8ConversionSlot = Nan::Null();
  }
  // end convert_to_v8 block
      info.GetReturnValue().Set(v8ConversionSlot);
    }
    // end field block
     // start field block
    NAN_METHOD(GitCertX509::Len) {
      v8::Local<v8::Value> v8ConversionSlot;

            size_t
           len =
          Nan::ObjectWrap::Unwrap<GitCertX509>(info.This())->GetValue()->len;
 // start convert_to_v8 block
     v8ConversionSlot = Nan::New<Number>( len);
  // end convert_to_v8 block
      info.GetReturnValue().Set(v8ConversionSlot);
    }
    // end field block
  // force base class template instantiation, to make sure we get all the
// methods, statics, etc.
template class NodeGitWrapper<GitCertX509Traits>;
 