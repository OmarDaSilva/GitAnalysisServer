// This is a generated file, modify: generate/templates/templates/class_content.cc

#include <nan.h>
#include <string.h>

extern "C" {
  #include <git2.h>
 }

#include "../include/nodegit.h"
#include "../include/lock_master.h"
#include "../include/functions/copy.h"
#include "../include/status_entry.h"
#include "nodegit_wrapper.cc"

  #include "../include/diff_delta.h"
 
#include <iostream>

using namespace std;
using namespace v8;
using namespace node;

  GitStatusEntry::~GitStatusEntry() {
    // this will cause an error if you have a non-self-freeing object that also needs
    // to save values. Since the object that will eventually free the object has no
    // way of knowing to free these values.
   }

  void GitStatusEntry::InitializeComponent(v8::Local<v8::Object> target, nodegit::Context *nodegitContext) {
    Nan::HandleScope scope;

    v8::Local<v8::External> nodegitExternal = Nan::New<v8::External>(nodegitContext);
    v8::Local<FunctionTemplate> tpl = Nan::New<FunctionTemplate>(JSNewFunction, nodegitExternal);

    tpl->InstanceTemplate()->SetInternalFieldCount(1);
    tpl->SetClassName(Nan::New("StatusEntry").ToLocalChecked());

         Nan::SetPrototypeMethod(tpl, "status", Status, nodegitExternal);
         Nan::SetPrototypeMethod(tpl, "headToIndex", HeadToIndex, nodegitExternal);
         Nan::SetPrototypeMethod(tpl, "indexToWorkdir", IndexToWorkdir, nodegitExternal);
  
    InitializeTemplate(tpl);

    v8::Local<Function> constructor_template = Nan::GetFunction(tpl).ToLocalChecked();
    nodegitContext->SaveToPersistent("GitStatusEntry::Template", constructor_template);
    Nan::Set(target, Nan::New("StatusEntry").ToLocalChecked(), constructor_template);
  }

      // start field block
    NAN_METHOD(GitStatusEntry::Status) {
      v8::Local<v8::Value> v8ConversionSlot;

            git_status_t
           status =
          Nan::ObjectWrap::Unwrap<GitStatusEntry>(info.This())->GetValue()->status;
 // start convert_to_v8 block
     v8ConversionSlot = Nan::New<Number>( status);
  // end convert_to_v8 block
      info.GetReturnValue().Set(v8ConversionSlot);
    }
    // end field block
     // start field block
    NAN_METHOD(GitStatusEntry::HeadToIndex) {
      v8::Local<v8::Value> v8ConversionSlot;

            git_diff_delta *
            head_to_index =
           Nan::ObjectWrap::Unwrap<GitStatusEntry>(info.This())->GetValue()->head_to_index;
 // start convert_to_v8 block
    if (head_to_index != NULL) {
      v8::Local<v8::Array> owners = Nan::New<Array>(0);
         Nan::Set(owners, owners->Length(), info.This());
          v8ConversionSlot = GitDiffDelta::New(
        head_to_index,
        false
          , owners
       );
   }
  else {
    v8ConversionSlot = Nan::Null();
  }
  // end convert_to_v8 block
      info.GetReturnValue().Set(v8ConversionSlot);
    }
    // end field block
     // start field block
    NAN_METHOD(GitStatusEntry::IndexToWorkdir) {
      v8::Local<v8::Value> v8ConversionSlot;

            git_diff_delta *
            index_to_workdir =
           Nan::ObjectWrap::Unwrap<GitStatusEntry>(info.This())->GetValue()->index_to_workdir;
 // start convert_to_v8 block
    if (index_to_workdir != NULL) {
      v8::Local<v8::Array> owners = Nan::New<Array>(0);
         Nan::Set(owners, owners->Length(), info.This());
          v8ConversionSlot = GitDiffDelta::New(
        index_to_workdir,
        false
          , owners
       );
   }
  else {
    v8ConversionSlot = Nan::Null();
  }
  // end convert_to_v8 block
      info.GetReturnValue().Set(v8ConversionSlot);
    }
    // end field block
  // force base class template instantiation, to make sure we get all the
// methods, statics, etc.
template class NodeGitWrapper<GitStatusEntryTraits>;
 