// This is a generated file, modify: generate/templates/templates/class_content.cc

#include <nan.h>
#include <string.h>

extern "C" {
  #include <git2.h>
 }

#include "../include/nodegit.h"
#include "../include/lock_master.h"
#include "../include/functions/copy.h"
#include "../include/strarray.h"
#include "nodegit_wrapper.cc"

  #include "../include/str_array_converter.h"
 
#include <iostream>

using namespace std;
using namespace v8;
using namespace node;

  GitStrarray::~GitStrarray() {
    // this will cause an error if you have a non-self-freeing object that also needs
    // to save values. Since the object that will eventually free the object has no
    // way of knowing to free these values.
       }

  void GitStrarray::InitializeComponent(v8::Local<v8::Object> target, nodegit::Context *nodegitContext) {
    Nan::HandleScope scope;

    v8::Local<v8::External> nodegitExternal = Nan::New<v8::External>(nodegitContext);
    v8::Local<FunctionTemplate> tpl = Nan::New<FunctionTemplate>(JSNewFunction, nodegitExternal);

    tpl->InstanceTemplate()->SetInternalFieldCount(1);
    tpl->SetClassName(Nan::New("Strarray").ToLocalChecked());

          Nan::SetPrototypeMethod(tpl, "copy", Copy, nodegitExternal);
           Nan::SetPrototypeMethod(tpl, "strings", Strings, nodegitExternal);
         Nan::SetPrototypeMethod(tpl, "count", Count, nodegitExternal);
  
    InitializeTemplate(tpl);

    v8::Local<Function> constructor_template = Nan::GetFunction(tpl).ToLocalChecked();
    nodegitContext->SaveToPersistent("GitStrarray::Template", constructor_template);
    Nan::Set(target, Nan::New("Strarray").ToLocalChecked(), constructor_template);
  }

  
/*
   * @param Strarray src
     * @return Number  result    */
NAN_METHOD(GitStrarray::Copy) {
  Nan::EscapableHandleScope scope;

  if (info.Length() == 0 || !(Nan::To<bool>(info[0]).FromJust())) {
    return Nan::ThrowError("Array, String Object, or string src is required.");
  }
// start convert_from_v8 block
  const git_strarray * from_src = NULL;

  from_src = StrArrayConverter::Convert(info[0]);
// end convert_from_v8 block

  git_error_clear();

  { // lock master scope start
    nodegit::LockMaster lockMaster(
      /*asyncAction: */false
            ,
              Nan::ObjectWrap::Unwrap<GitStrarray>(info.This())->GetValue()
            ,
              from_src
    );

 int result =     git_strarray_copy(
          Nan::ObjectWrap::Unwrap<GitStrarray>(info.This())->GetValue()
,          from_src
    );


      v8::Local<v8::Value> v8ConversionSlot;
// start convert_to_v8 block
     v8ConversionSlot = Nan::New<Number>( result);
  // end convert_to_v8 block
        return info.GetReturnValue().Set(scope.Escape(v8ConversionSlot));
  }
}
       // start field block
    NAN_METHOD(GitStrarray::Strings) {
      v8::Local<v8::Value> v8ConversionSlot;

            char **
           strings =
          Nan::ObjectWrap::Unwrap<GitStrarray>(info.This())->GetValue()->strings;
 // start convert_to_v8 block
  if (strings){
      v8ConversionSlot = Nan::New<v8::String>(*strings).ToLocalChecked();
   }
  else {
    v8ConversionSlot = Nan::Null();
  }

  // end convert_to_v8 block
      info.GetReturnValue().Set(v8ConversionSlot);
    }
    // end field block
     // start field block
    NAN_METHOD(GitStrarray::Count) {
      v8::Local<v8::Value> v8ConversionSlot;

            size_t
           count =
          Nan::ObjectWrap::Unwrap<GitStrarray>(info.This())->GetValue()->count;
 // start convert_to_v8 block
     v8ConversionSlot = Nan::New<Number>( count);
  // end convert_to_v8 block
      info.GetReturnValue().Set(v8ConversionSlot);
    }
    // end field block
  // force base class template instantiation, to make sure we get all the
// methods, statics, etc.
template class NodeGitWrapper<GitStrarrayTraits>;
 